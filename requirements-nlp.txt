-r requirements.txt

spacy==3.7.2
# Pinned by URL rather than fetched with `spacy download`, which resolves the
# version through a compatibility lookup at build time and breaks the build
# when that lookup fails.
https://github.com/explosion/spacy-models/releases/download/en_core_web_sm-3.7.0/en_core_web_sm-3.7.0-py3-none-any.whl
transformers==4.51.3
torch==2.1.1
# Sentence embeddings for semantic retrieval. 2.2.2 imports a huggingface_hub
# symbol that newer releases removed, so the pin matters.
sentence-transformers==2.7.0
# The query API is served from this image because both models already live
# here. waitress rather than Flask's development server, which is not meant
# to face anything.
flask==3.0.0
waitress==2.1.2
# Phi-4-mini's architecture needs a tokenizer conversion path that older
# releases lack.
sentencepiece==0.2.0
accelerate==0.34.2
scikit-learn==1.3.2
numpy==1.26.2
